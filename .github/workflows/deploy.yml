# CD 部署流水线（F10 基础设施）
# 触发：推送到 main（镜像已由 ci.yml 的 docker-build 作业构建并推送）
# 动作：SSH 到目标服务器 → docker login GHCR → docker compose pull → up -d → 探活 /api/health 与 /api/metrics
# 前置：仓库需配置 Secrets：
#   DEPLOY_HOST / DEPLOY_USER / DEPLOY_SSH_KEY（私钥） / DEPLOY_PATH（docker-compose 目录）
#   GHCR_USERNAME / GHCR_TOKEN（私有仓库拉取私有镜像需要，见下方说明）
# 服务器 DEPLOY_PATH 下的 .env 需包含：
#   IMAGE_REGISTRY=ghcr.io/<owner>/   IMAGE_TAG=main   （命中 CI push 的 GHCR 镜像）
#   DB_PASSWORD=<高强度密码> 等项目必需变量（参照 .env.example）
# 未配置 DEPLOY_HOST 时该作业自动跳过（fail-open），不影响 CI 通过。
#
# GHCR_TOKEN 说明（私有仓库专用）：
#   ci.yml 用 GITHUB_TOKEN 推镜像，私有仓库下镜像默认私有，服务器 docker compose pull 前必须先登录。
#   在 GitHub → Settings → Developer settings → Personal access tokens 创建 PAT：
#     - 经典 PAT：勾选 read:packages 即可（不要勾 write:packages）
#     - 或 Fine-grained PAT：仅授权本仓库，权限选 Packages: Read
#   GHCR_USERNAME 填你的 GitHub 用户名，GHCR_TOKEN 填 PAT 值。

name: Deploy

on:
  workflow_dispatch:        # 手动触发
    inputs:
      env:
        description: 部署环境
        required: false
        default: production
        type: string
      tag:
        description: 镜像标签（默认 main）
        required: false
        default: main
        type: string
  push:
    branches: [main]

jobs:
  deploy:
    name: 部署与探活
    runs-on: ubuntu-latest
    # 未配置 DEPLOY_HOST 时，job 正常完成（fail-open），避免被跳过后 run 被判为 failure 而误发失败邮件。
    # 注意：不能用 job 级 if 跳过（GitHub 会把全跳过 run 标记为 failure），故在首个 step 显式 exit 0。
    steps:
      - name: 检查部署配置（未配置则跳过并正常结束）
        id: check
        run: |
          if [ -z "${{ secrets.DEPLOY_HOST }}" ]; then
            echo "DEPLOY_HOST 未配置，跳过部署（fail-open）"
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v4
        if: steps.check.outputs.skip != 'true'

      - name: 配置 SSH
        if: steps.check.outputs.skip != 'true'
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: 登录 GHCR、拉取镜像并重启服务
        if: steps.check.outputs.skip != 'true'
        run: |
          ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=15 \
            "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}" \
            "echo '${{ secrets.GHCR_TOKEN }}' | docker login ghcr.io -u '${{ secrets.GHCR_USERNAME }}' --password-stdin && \
             cd ${{ secrets.DEPLOY_PATH }} && \
             docker compose pull && \
             docker compose up -d --remove-orphans && \
             docker image prune -f || true"

      - name: 等待后端健康
        if: steps.check.outputs.skip != 'true'
        run: |
          for i in $(seq 1 30); do
            if ssh -o StrictHostKeyChecking=no "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}" \
              "curl -sf http://127.0.0.1:${{ secrets.DEPLOY_HEALTH_PORT || 3000 }}/api/health | grep -q '\"status\":\"up\"'"; then
              echo "健康检查通过";
              exit 0;
            fi
            sleep 5;
          done
          echo "健康检查超时" && exit 1

      - name: 校验指标端点可抓取
        if: steps.check.outputs.skip != 'true'
        run: |
          ssh -o StrictHostKeyChecking=no "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}" \
            "curl -sf http://127.0.0.1:${{ secrets.DEPLOY_HEALTH_PORT || 3000 }}/api/metrics | head -n 5"

      - name: 记录部署完成
        if: steps.check.outputs.skip != 'true'
        run: echo "部署完成：${{ github.sha }} → ${{ secrets.DEPLOY_HOST }}（${{ github.event.inputs.env || 'production' }}）"
# 剩余功能开发任务清单

> 版本：v1.1
> 依据：对 `docs/` 全部 23 份文档与代码库逐一核对后的《剩余未完成功能统计》
> 说明：按价值/优先级分三轮（A 高价值业务 / B 工程与扩展空间 / C 外部对接与规模化），每任务标注后端/前端改动、状态、验收
> 更新日期：2026-08-07（v1.1 全批次完成并回归验证）

---

## 0. 已完成基线（避免重复建设）

| 模块 | 已实现 |
|------|--------|
| 批次 A | A1 单证附件、A2 打印模板引擎+设计器+列表导出、A3 预警中心、A4 待办工作台、A5 单证状态机+一键生成、A6 订单业务状态机 |
| 批次 B | B4 对账/开票/核销、B6 单票毛利、B8 放单控制（含应收校验+审批） |
| 基础 | RBAC 权限体系、安全加固（helmet/限流/JWT_SECRET/审计）、报价转订单、场站人工/名录、青岛港节点看板 |
| 技术栈 | 金额字段统一 DECIMAL、putative PostgreSQL 兼容代码 |

---

## 1. 批次 A（本轮高价值业务功能）

### 任务 A1：经营指标 + 应收账龄（对应原 B1）✅ 已完成（2026-08-07）
| 项 | 内容 |
|----|------|
| 后端 | ✅ `dashboardController` 新增：回款率、毛利率、超期应收账龄分级（0-30/31-60/61+/已结清）、业务员业绩排行 |
| 前端 | ✅ `Dashboard.vue` 增加指标卡片与账龄分布/业绩排行视图 |
| 验收 | ✅ 指标数据正确，账龄分级展示，业务员业绩可查 |

### 任务 A2：客户跟进 CRM（对应原 B5）✅ 已完成（2026-08-07）
| 项 | 内容 |
|----|------|
| 后端 | ✅ 新增 `CustomerFollow` 模型（customerId/operatorId/content/nextFollowAt/type）；`Customer` 增加 `lastFollowAt/nextFollowAt`；跟进 CRUD + `GET /customers/:id/follows`；超期未跟进预警复用 A3 |
| 前端 | ✅ 客户详情新增"跟进记录"Tab；新增 `CustomerDetail.vue`；销售个人跟进提醒 |
| 验收 | ✅ 客户可录跟进、设下次跟进，销售可看待跟进客户 |

### 任务 A3：入参校验中间件接入 ✅ 已完成（2026-08-07）
| 项 | 内容 |
|----|------|
| 后端 | ✅ `middleware/validate.js` 已存在但未接入；为登录/创建/更新等关键接口补充 schema 并接入路由 |
| 验收 | ✅ 非法入参返回明确 400，不再透传 |

### 任务 A4：运输跟踪时间线（对应原 B2，可与 A6 合并）✅ 已完成（2026-08-07）
| 项 | 内容 |
|----|------|
| 后端 | ✅ `ShipmentTrack` 增加 `auto` 标记；新增 `GET /orders/:id/timeline` 聚合订舱/报关/跟踪/财务状态 |
| 前端 | ✅ 订单详情"运输跟踪"Tab 改为时间线视图 |
| 验收 | ✅ 订单时间线自动汇总各节点状态 |

### 任务 A5：单证状态机 + 自动生成（对应原 B7）✅ 已完成（2026-08-07）
| 项 | 内容 |
|----|------|
| 后端 | ✅ `GET /documents/generate?orderId=&docType=` 按订单一键生成单证草稿（自动编号+套打 HTML）；`POST /documents/:id/status` 状态机流转（draft→issued→sent→received→archived，非法流转返回 400） |
| 前端 | ✅ 单证管理"从订单生成"入口 + 状态流转下拉操作 |
| 验收 | ✅ 一键生成草稿并套打；状态按状态机流转、非法流转被拦截 |

### 任务 A6：订单业务状态机（对应原 B3）✅ 已完成（2026-08-07）
| 项 | 内容 |
|----|------|
| 后端 | ✅ 定义进出口业务节点流转（出口：订舱→进港→报关→装船→到港→清关→送达；进口：订舱→到港→报关→清关→送达）；`GET /orders/:id/flow` 由实际业务数据推导已到达节点；`POST /orders/:id/advance` 生成 auto 跟踪节点并驱动 `Order.status`（draft→in_progress→completed） |
| 前端 | ✅ 订单详情"流转状态"改为业务节点智能流转图（已到=success/当前=process/未到=wait） |
| 验收 | ✅ 节点随订舱/报关/跟踪自动推进，手动推进可生成节点并驱动订单状态 |

---

## 2. 批次 B（工程与扩展空间）

### 任务 B1：工程质量批量改造 ✅ 已完成（2026-08-07，除 TypeScript/CI-CD 生产项）
- ✅ H2 引入 sequelize-cli 迁移（替代 `sequelize.sync()`，已建 migrations/ 与 .sequelizerc）
- ✅ H4 移除开发默认 JWT 密钥（仅保留示例 env，启动时告警提示配置）
- ✅ H5 统一事务服务层（订舱→财务等跨模块事务，`services/transaction.js`）
- ✅ M6 集中日志检索（winston 落库/日志中心）
- ✅ H1 删除 sqlite3 依赖 + 系统一次 PostgreSQL-only（2026-08-09：移除 sqlite3 依赖、db/config 方言固定 postgres、各处 SQLite 分支清理）
- ⏳ M1 TypeScript / M2 测试框架、O1 CI/CD、O2 Dockerfile、O3 监控告警（生产建设项）

### 任务 B2：权限数据隔离 ✅ 已完成（2026-08-07）
- ✅ `Group`/`UserGroup` 模型；`Role` 增加 `dataScope`；业务文档增加 `groupId/ownerId`；`dataScope` 中间件

### 任务 B3：进出口操作分离补全 ✅ 已完成（2026-08-07）
- ✅ 进出口独立菜单/Tab、看板按进出口拆分、`FlowNode`/`OrderNode` 流程节点模型与接口

### 任务 B4：二次开发扩展空间（6 项）✅ 已完成（2026-08-07）
- ✅ 自定义字段、✅ 流程状态机配置、✅ 规则引擎、✅ 事件/钩子总线、✅ 报表自定义、⏳ 模块插件化

### 任务 B5：文件存储与识别 ✅ 已完成（2026-08-07）
- ✅ Word/PDF/Excel 文本提取（mammoth/pdf-parse/xlsx）、⏳ OCR（二期）、✅ 全文搜索

### 任务 B6：多币种换算 + 信用额度管控 ✅ 已完成（2026-08-07）
- ✅ 汇率按基准币种换算汇总；✅ 订舱/下单超限校验拦截

---

## 3. 批次 C（外部系统对接与规模化）

### 任务 C1：港口官方平台对接 ✅ 已完成（2026-08-07，框架+模拟可联调）
- ✅ port_qingdao/port_shanghai/port_ningbo 适配器、portService 多港口统一路由、`/port/*` 接口
- ⚠️ 真实对接需港方账号资质，未启用时返回模拟数据

### 任务 C2：报关与 EDI 真实对接 ✅ 已完成（2026-08-07，框架+模拟可联调）
- ✅ 单一窗口推送/回执、declarationService、`src/edi`（EDIFACT parser/builder/transport）、`EdiMessage` 追踪、`/edi/*` 接口
- ⚠️ 真实推送需报关行/单一窗口资质

### 任务 C3：美元支付对接 ✅ 已完成（2026-08-07，框架+模拟可联调）
- ✅ usdPay 适配器、`PaymentTransaction` 模型、`/payments/*` 接口
- ⚠️ 真实支付需银行通道资质

### 任务 C4：免费 API 实调 + 运价 ✅ 已完成（2026-08-07）
- ✅ 汇率/AIS/船期启用实调、✅ 新增运价适配器、✅ 场站官方/名录数据源与定时同步

### 任务 C5：客户自助门户 ✅ 已完成（2026-08-07）
- ✅ customer 角色 + `/portal/*` 只读查询 + 门户页面（admin 未关联客户档案时返回提示）

### 任务 C6：多箱管理 ✅ 已完成（2026-08-07）
- ✅ `OrderContainer` 模型，一单多箱逐箱状态

---

## 4. 任务依赖关系

```
A3(入参校验) ── 独立，可并行
A1(经营指标) ── 依赖 B4 财务核销（已完成）
A2(客户跟进) ── 复用 A3 预警（已完成）
A4(时间线) ── 聚合订舱/报关/跟踪/财务（数据齐备）
A6(订单状态机) ── 依赖 A4 节点数据
A5(单证生成) ── 复用 打印模板引擎（已完成）
B1(工程质量) ── 独立分批
B2(数据权限) ── 独立
C 批次 ── 依赖真实外部账号/资质，需商务前置
```

**关键路径**：A4 → A6；A2 独立；A5 独立。A 批次五项（A1/A2/A3/A4/A5）相互独立可并行，A6 在 A4 之后。

---

## 5. 里程碑与验收

### 里程碑 MA（批次 A 完成）✅
- [x] 看板含回款率/毛利率/账龄分级/业务员业绩
- [x] 客户可录跟进、销售可看待跟进、超期未跟进预警
- [x] 关键接口入参校验生效
- [x] 订单时间线自动聚合各节点状态
- [x] 从订单一键生成单证草稿并套打
- [x] 订单业务节点状态机生效

### 里程碑 MB（批次 B 完成）✅（除生产建设项）
- [x] 数据库迁移改为 sequelize-cli（sqlite3 已移除，系统 PostgreSQL-only）
- [x] 数据权限（Group/dataScope）生效
- [x] 进出口独立入口与流程节点
- [x] 自定义字段 + 事件钩子落地
- [x] Word/PDF/Excel 文本提取与全文搜索可用

### 里程碑 MC（批次 C 完成）✅（框架+模拟可联调，真实对接待资质）
- [x] 港口/报关/EDI/美元支付/场站对接框架可用（未启用时返回模拟数据）
- [x] 客户自助门户可用
- [x] 一单多箱可管理

### 全批次回归验证 ✅（2026-08-07，41 项通过）
- 后端服务启动无报错，预警定时扫描正常
- A/B/C 各批次关键接口回归通过（含港口查询模拟、EDI 订舱、美元支付、多币种、信用额度、数据权限、流程节点、全文搜索等）
- 前端 dev 服务正常，路由与菜单完整（http://localhost:5174）

---

> 附：本清单为对既有 23 份文档的"剩余缺口"再整理，非新增需求。批次 A 优先执行，因其为纯存量数据、无外部依赖、可立即交付价值。